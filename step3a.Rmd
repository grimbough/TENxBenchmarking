---
title: "rhdf5 tweaks"
author: "Mike Smith"
output: html_document
---

## Installing developmental version of **DelayedArray**
 
```{r, installBranch, results = 'hide', message = FALSE, warning = FALSE}

if(packageVersion('rhdf5') != "2.23.0") {
    BiocInstaller::biocLite('rhdf5', suppressUpdates = TRUE, ask = FALSE)
}
if(packageVersion('DelayedArray') != "0.5.1.1") {
    devtools::install_github(repo = "grimbough/DelayedArray", ref = "d61695b",  quiet = TRUE, force = TRUE)
}
```

```{r, load-libs, message=FALSE}
library(TENxBrainData)
library(microbenchmark)
options(DelayedArray.block.size=2e9)
```

```{r, load-data, message=FALSE}
tenx <- TENxBrainData()
tenx.sub <- tenx[,1:13000]
```

```{r, save-results}
system.time(res3a <- colSums(counts(tenx.sub)))
save(res3a, file = "/media/Storage/Work/TENxBenchmarking/res3a.rda")
```

```{r, DelayedArray-dev}
bm3a <- microbenchmark(colSums(counts(tenx.sub)), 
                      times = 5L, unit = "s",
                      control = list(order = "block", warmup = 0))
bm3a$expr <- "DelayedArray"
save(bm3a, file = "/media/Storage/Work/TENxBenchmarking/bm3a.rda")
bm3a
```

```{r, sessionInfo}
sessionInfo()
```